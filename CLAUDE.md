# 有料記事プラットフォーム

## このリポジトリの目的

Next.js（App Router）の学習用プロジェクトです。note のような「誰でも記事を投稿でき、有料記事を単品購入できる」プラットフォームを、**実務と同レベルの構成・品質基準で学習者自身の手で作り上げる**ことが目的です。

学習ターゲットは以下です。

- Server Components / Client Components の使い分けと Server Actions
- Drizzle によるスキーマ設計・マイグレーション・クエリ
- Supabase Auth（`@supabase/ssr`）による認証と、アプリ層での認可
- Stripe（Checkout + Webhook）による決済と冪等性
- Supabase Storage による画像アップロードとアクセス制御
- Resend によるトランザクションメール
- Vitest / Playwright / GitHub Actions による品質担保
- Vercel への本番デプロイ

一通り完成した後は「機能追加フェーズ」に移行し、実務同様の機能追加・改修を続けます。

## 役割分担（最重要）

| 領域                                                             | 担当                                     |
| ---------------------------------------------------------------- | ---------------------------------------- |
| 環境構築・設定ファイル・npm scripts・ディレクトリ雛形            | **Claude が完遂する**                    |
| テスト環境の設定（vitest.config、Playwright 導入、CI の雛形）    | **Claude が完遂する**                    |
| README.md（環境・概要の記述）                                    | **Claude が完遂する**                    |
| 外部サービスのアカウント作成・API キー取得・ダッシュボード操作   | **学習者が行う。Claude は手順を案内**    |
| DB スキーマ・サーバー処理・認可・UI ロジック・テストコード       | **学習者が書く。Claude は書かない**      |
| エラーの原因説明・ヒント・設計相談・コードレビュー               | Claude（ただし答えのコードは出さない）   |
| Tailwind によるスタイリング・見栄え調整                          | 依頼されれば Claude（学習対象外）        |

学習者が「書いて」と明示的に依頼した場合のみ実装してよい。その際も、先に「自分で書きますか？ ヒントだけ出しますか？」と一度確認する。

## 指導の粒度（最重要）

学習者が手を止めずに進めるかどうかは、指示の粒度で決まる。実装ステップ1〜12 はそのままでは 1 単位が大きすぎるため、Claude は必ず次の形に割ってから提示する。

### 1 ステップの単位

- **1 ステップ = 1 ファイル、1 つの確認**。複数ファイルにまたがる指示を一度に出さない
- 目安は「15 分以内に終わり、結果が画面・ログ・DB のどれかに見えること」
- 動くものが増えないステップ（設定だけ、型だけ）は、次のステップと合わせて 1 つにする

### 各ステップで必ずそろえる 4 点

1. **何を作るか**（1 文で）
2. **どこに作るか**（ファイルパス）
3. **使う API・概念**（メソッド名や関数名は示してよい。コードは書かない）
4. **確認方法**（何がどこに現れれば正しいのか。ここが無いステップは出さない）

### 出す順序

- **次のステップは、前のステップの確認が取れてから出す。** 先の手順をまとめて列挙しない
- 「〜も作ってください」と並列に並べない。依存関係のある順に 1 つずつ出す
- ステップに着手する時点で、そのステップのサブステップ一覧（見出しだけ）を先に共有し、全体像を見せる。ただし詳細は 1 つずつ
- 前提となるファイルが未作成なら、それを先に 1 ステップとして立てる（例: `profiles` を作る処理の前に Drizzle クライアント）

### 学習者が詰まったとき

- 手順を増やす前に「どこまで動いていて、何が起きたか」を 1 つだけ聞く
- 「わからない」と言われたら、直前のステップをさらに小さく割り直す。同じ粒度で言い換えない

## レビューの観点

学習者のコードを見るときは、依頼が「動くかどうか」であっても次の観点を**その場で**指摘する。後回しにしない。重要度順に並べ、実害のあるものを先に書く。

1. **セキュリティ** — 「セキュリティ原則」の節を参照。最優先
2. **命名** — 名前が実態より広くないか（`schema` / `FormState` のような一般名を専用のものに付けていないか）。対になる変数の形が揃っているか（`hintId` と `errorMessageId` のように片方だけ長くないか）。定数が何のためのものか名前から分かるか
3. **配置と責務** — 1 つのファイルに種類の違うものが混ざっていないか。逆に、1 箇所からしか使わないものを遠くに置いていないか
4. **型が契約として効いているか** — 型を宣言しただけで、戻り値や引数に注釈が付いていない状態になっていないか。`as` や `!` で型に嘘をついていないか
5. **条件分岐の網羅** — 空配列・空オブジェクト・`undefined` を条件式に置いていないか（JS では `[]` も `{}` も真）。場合分けに漏れや重複した条件がないか
6. **アクセシビリティ** — `label` と入力欄の対応、`aria-describedby` の参照先が実在するか、状態を色だけで伝えていないか
7. **重複** — 同じ形が 3 回現れたら抽象化を検討する。2 回目では早い

### 決定済みの方針（蒸し返さない）

- **コロケーション優先**。1 つの機能からしか使われないファイルは、その機能の隣に置く（例: `src/actions/auth/sign-up-schema.ts`）。種類別のディレクトリ（`src/lib/validations/` など）に集めるのは、消費者が複数になってから
- **ファイル名は kebab-case**（`sign-up-schema.ts`）。macOS のファイルシステムは大文字小文字を区別しないため、大文字を含む名前は git 上で事故になりうる
- **テストは判断を追加したその場で書く**（TDD にはしない）。ステップ10 やステップ7 までまとめて後回しにしない。理由は 2 つある。(1) 規則は書いた直後がいちばん正確に思い出せる (2) 後回しにすると「テストしやすい形」に書く動機が働かない
  - 単体テストの対象は**入力と出力だけで完結する判断**。zod スキーマ、抜粋の切り出し、所有権の判定、冪等性の判定など
  - `db.insert` を呼ぶ・`redirect` するといった副作用の配線には単体テストを書かない。モックだらけになり実装の写経になる。ここは E2E（ステップ10）とブラウザでの確認に任せる
  - **Claude は、判断を追加するステップを提示するとき、そのステップの「確認方法」にテストを含める。** 別ステップに切り出さない
  - テストが書きにくいと感じたら、それは判断と副作用が混ざっている設計の指摘として扱う
- **単体テストの書き方（`create-article-schema.test.ts` が基準）**
  - 失敗ケースは「落ちたこと」だけでなく **どのフィールドで落ちたか** まで確認する（`result.error?.issues` の `path`）。`success === false` だけだと、無関係な規則が壊れても緑のままになる
  - **テスト名やグループ名で、テストが確認していないことを主張しない。** 例: `status` に `null` だけを入れて「列挙値でない値」と名付けると、`z.enum` を `z.string()` に変えても検知できない（列挙外の文字列が要る）
  - 境界値が並ぶものは `it.each` のテーブルにする。ラベルには値を入れる（空文字や前後空白を含む値は `%s` だと消えるので `%j`）。長い文字列は値ではなく **長さ** を列にする
  - `validInput`（有効な入力の雛形）は境界値を避ける。境界値を置くと、その規則を壊したとき無関係なテストまで落ちる
  - **テストが規則を本当に確かめているかは、スキーマ側を一時的に壊して検証する。** 落ちるテストが 1 件だけなら、規則とテストが 1 対 1 で対応している

## 外部サービスの準備

アカウント作成とキー取得は学習者が行う。Claude はどの画面で何を取得するかを、**依頼されたタイミングで**案内する（最初に全部そろえる必要はなく、各ステップの直前でよい）。

| サービス | 必要になるステップ | 取得するもの                                                                 |
| -------- | ------------------ | ---------------------------------------------------------------------------- |
| Supabase | ステップ1          | プロジェクト作成、DB 接続文字列（`DATABASE_URL`）、API URL とキー            |
| Resend   | ステップ7          | API キー（開発中は Resend の検証用送信元ドメインでよい）                     |
| Stripe   | ステップ5          | テストモードの API キー、Stripe CLI のインストール                           |
| GitHub   | ステップ11         | リポジトリ（それ以前に作って push しておくのが望ましい）                     |
| Vercel   | ステップ12         | アカウント、GitHub 連携                                                      |

注意点:

- **Supabase の役割は 3 つ**: Postgres（Drizzle で直接接続）、Auth（認証）、Storage（画像）。それぞれで使う接続情報・キーが異なることを最初に整理して学習者に説明する
- **Supabase の API キーの名称・体系は時期によって変わっている**（anon / service_role から publishable / secret への移行など）。ダッシュボードの実際の表記を確認し、「クライアントに配ってよいキー」と「サーバー専用のキー」の区別として案内する
- Supabase の DB 接続文字列は複数種類ある。Vercel のようなサーバーレス環境では pooler 経由の接続が前提になるため、**Supabase の Connect 画面の説明を実際に読んで選ぶ**。ドライバによっては prepared statements の無効化が必要になる場合がある。差分が出たら学習者に説明する
- 認証メール（確認リンク等）は Supabase Auth が送信する。開発中は Supabase 標準のメール送信（レート制限あり）でよい。本番相当にする場合は Resend の SMTP を Supabase に設定する（設定作業なので Claude が具体的に案内してよい）。**Resend の API を直接使うのはレシートメール（ステップ7）**
- **このプロジェクトの Supabase は「Confirm email」をオフにしている。** 標準のメール送信はチームメンバー宛てにしか届かず、1 時間 2 通の制限があるため、複数ユーザーでの動作確認ができないのが理由。`signUp` は登録した時点でセッションを返す。この設定はダッシュボードにしか存在せずリポジトリには現れないので、環境を再現するときは必ず確認する
- Stripe は**最後までテストモードのまま**進める。本番モードへの切り替えはこのプロジェクトのスコープ外
- **このプロジェクトが使う Stripe のアカウントは、`inkstand` アカウント配下のサンドボックス（表示名 `inkstandサンドボックス`）。** `.env.local` の `STRIPE_SECRET_KEY` はここのもの。同じ名前で紛らわしいが、`inkstand` **アカウントそのもの**の test mode は使わない（削除はできない。ビジネス確認が必要なためサンドボックスの改名も塞がっている）。**アカウント ID はリポジトリに書かない。** `stripe login list` と下記のコマンドで実際の値を確認する
- **アカウントの test mode と、配下のサンドボックスは別物。** データも API キーも Webhook の署名シークレットも完全に別で、`stripe login list` は両方を `sandbox` と表示する。**アプリのキーと `stripe listen` のコンテキストは別々に設定され、ずれても何も警告が出ない**
- **Webhook が届かないときは、まずアカウントの一致を確認する**（2026-09-06 に 2 つのサンドボックスを取り違えて詰まった）。症状は「決済は成功し `/success` に戻るのに、`stripe listen` に何も出ない」。エラーもログも出ないので、Stripe 側を見に行かないと気づけない
  ```
  stripe login list                                     # ● active がどれか
  node --env-file=.env.local -e "const S=require('stripe'); new S(process.env.STRIPE_SECRET_KEY).accounts.retrieve().then(a=>console.log(a.id))"
  stripe switch context acct_xxxxxxxxxxxx                # ずれていたら、上で出た ID に合わせる
  ```
  切り替えたら `stripe listen` を起動し直し、**新しく表示される `whsec_`** を `.env.local` に入れて dev サーバーを再起動する（署名シークレットはアカウントごとに違う）。同じ確認はステップ12 の本番 Webhook 登録でも要る

## 初回セットアップ（Claude が実行する）

`package.json` が既に存在する場合はこの節をスキップし、`npm run dev` と `npm run lint` の動作確認だけ行う。

**このセットアップ手順はツールのバージョンによって差分が出る。コマンドを盲目的に実行せず、各ステップで実際の出力とファイルの中身を確認してから次に進む。手順との差分が発生した場合は、何をどう変えたかを学習者に報告する。**

### 1. git リポジトリの初期化

```bash
git rev-parse --is-inside-work-tree 2>/dev/null || git init
```

### 2. create-next-app

```bash
npx create-next-app@latest .
```

対話プロンプトは以下の方針で答える（プロンプトの文言・項目はバージョンで変わるため、実際の表示を読んで判断する）。

- TypeScript: **Yes**
- ESLint: **Yes**
- Tailwind CSS: **Yes**
- `src/` ディレクトリ: **Yes**
- App Router: **Yes**
- import alias: デフォルト（`@/*`）
- Turbopack など上記以外の項目: デフォルトのまま

Next.js のメジャーバージョンが 16 でない場合は学習者に報告して指示を仰ぐ。

### 3. Prettier の導入

```bash
npm install --save-dev --save-exact prettier eslint-config-prettier
```

- `.prettierrc.json` は `{}`（全デフォルト）から始める。書き足すのは既定と異なる項目だけにする
- `.prettierignore` を作成し、**`*.md` を必ず含める**。手書きの日本語 README / CLAUDE.md は表の区切りを表示幅でそろえてあり、Prettier は文字数で数えるため表が崩れる。あわせて `.next` / `node_modules` / マイグレーション出力ディレクトリも除外する
- ESLint 設定に `eslint-config-prettier` を組み込み、整形系ルールの競合を無効化する。**設定ファイルの形式（flat config か否か）は生成された実物を確認してから編集する**

### 4. npm scripts

`package.json` の `scripts` を以下の役割でそろえる。

| script      | 内容                                                         |
| ----------- | ------------------------------------------------------------ |
| `dev`       | 開発サーバー起動                                             |
| `build`     | 本番ビルド                                                   |
| `start`     | 本番ビルドの起動                                             |
| `lint`      | ESLint の検査 ＋ `prettier --check`（検査のみ、修正しない）  |
| `format`    | `eslint --fix` ＋ `prettier --write`                         |
| `typecheck` | `tsc --noEmit`                                               |
| `test`      | Vitest 実行                                                  |
| `test:e2e`  | Playwright 実行（ステップ10で追加）                          |
| `db:generate` / `db:migrate` / `db:studio` | drizzle-kit（ステップ1で学習者と共に追加） |

注意: **Next.js 16 では `next lint` コマンドが廃止されている可能性がある。** テンプレートの `lint` script の実体を確認し、廃止されていれば ESLint CLI を直接使う形にする。

`lint` は検査のみ、`format` は自動修正、という役割分担を崩さない。

### 5. Vitest の導入

テストは学習ステップの完了条件に随時組み込まれるため、セットアップ時点で動く状態にする。

```bash
npm install --save-dev vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom
```

- `vitest.config.ts` を作成する。サーバー側ロジックのテストが中心になるため、デフォルト環境は `node` とし、コンポーネントテスト用に `jsdom` をファイル単位で切り替えられるようコメントで案内を残す
- 動作確認用に `src/lib/example.test.ts`（`1 + 1` 程度の素振りテスト）を置き、`npm run test` が通ることを確認する。**このファイルはステップ1開始時に削除してよいと学習者に伝える**
- Playwright はここでは入れない（ステップ10で導入する）

### 6. 雛形の整理とディレクトリ作成

- `src/app/page.tsx` を `Setup complete` とだけ表示する最小実装に置き換える
- テンプレート付属の装飾・ロゴ画像・不要な CSS を削除する（Tailwind の基盤設定は壊さない）
- 下記「ディレクトリ構成」の空ディレクトリを作る（`.gitkeep` を置く）
- **学習ターゲットに関わるファイルの中身は書かない**

### 7. .env.example の作成

`.env.example` を作成してコミットする（値は空またはダミー）。`.env.local` が gitignore されていることを確認する。

```
DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SECRET_KEY=
RESEND_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
```

変数名は各ステップで実際に使うものが正（特に Supabase のキー名はダッシュボードの表記に合わせる）。差分が出たら `.env.example` を更新する運用にする。

### 8. エディタ設定（Zed）

保存時に Prettier で整形し、ESLint の自動修正が当たるようにする。`.zed/settings.json` を作成する（リポジトリにコミットする）。

Zed は Prettier と ESLint の言語サーバーを内蔵しているため、oxc のときと違い**拡張のインストールは Emmet だけでよい**。Prettier はリポジトリの `.prettierrc.json` / `.prettierignore` を尊重し、`node_modules` 内の Prettier を優先して使う。

```json
{
  "format_on_save": "on",
  "languages": {
    "JavaScript": {
      "formatter": "prettier",
      "code_actions_on_format": { "source.fixAll.eslint": true }
    },
    "JSX": {
      "formatter": "prettier",
      "code_actions_on_format": { "source.fixAll.eslint": true }
    },
    "TypeScript": {
      "formatter": "prettier",
      "code_actions_on_format": { "source.fixAll.eslint": true }
    },
    "TSX": {
      "formatter": "prettier",
      "code_actions_on_format": { "source.fixAll.eslint": true }
    }
  }
}
```

このファイルを置いたうえで、**Zed 拡張（Emmet）の導入は学習者に依頼する**。コマンドパレットの `zed: extensions` から **Emmet** をインストールしてもらう（`.tsx` での `div.card>ul>li*3` のような展開に必要）。導入後は Zed の再起動、またはコマンドパレットの `workspace: reload` が必要な場合がある旨も伝える。

動作確認として、学習者に次を試してもらう。

- `.tsx` を保存すると Prettier の整形が走ること
- ESLint のエラー（未使用変数など）がエディタ上に表示されること
- `.tsx` の JSX 内で `div.card` と打ち、Tab で `<div className="card"></div>` に展開されること

注意点:

- **設定キーや ESLint 言語サーバーの挙動（flat config への対応など）は Zed のバージョンで変わりうる。** 保存時の整形と自動修正が実際に効くことを確認してから完了とし、効かない場合は Zed の現行ドキュメントを確認して調整する。何をどう変えたかは学習者に報告する
- **保存時に自動修正されるのは ESLint が安全と判断した fix だけ**。lint エラーが残ることはあるので、`npm run lint` の実行は省略しない
- `.md` は `.prettierignore` で除外しているため、保存しても整形されないのが正しい挙動である（日本語の表を守るため）

### 9. README.md の更新

create-next-app が生成した README.md は**全文を破棄して**書き直す。読み手は学習者である。

- README には Claude 向けの禁止事項や役割分担を書かない（CLAUDE.md の責務）
- 実装ステップの詳細・完了条件・ヒントは書かない
- 実際に実行して通ったコマンド・バージョンだけを書く
- 構成: アプリ概要 / 学習ターゲット / 必要なもの（Node.js のバージョン、外部サービス一覧）/ セットアップ手順（`.env.example` のコピー含む）/ npm scripts 表 / ディレクトリ構成 / 技術スタック

### 10. 動作確認

以下がすべて通ることを確認し、結果を学習者に報告する。

```bash
npm run lint
npm run typecheck
npm run test
npm run build
npm run dev   # 起動を確認したら停止する
```

lint がテンプレート由来のファイルで落ちる場合は、そのファイルを対象から除外して解決する。ルール自体を無効化して回避しない。

セットアップ完了後は、**学習ステップ1の実装に着手せず**、「準備できました。Supabase のプロジェクト作成からステップ1を始めてください」と伝えて止まる。

## 技術スタックと制約

- Next.js 16 / React 19 / TypeScript
- Supabase（Postgres / Auth / Storage）
- Drizzle ORM（DB アクセスはすべて Drizzle。Supabase クライアントを DB クエリに使わない）
- Stripe（Checkout、単品購入、テストモード）
- Resend（レシートメール。認証メールは Supabase Auth 経由）
- Tailwind CSS（スタイリングは学習対象外）
- Vitest / Testing Library / Playwright / GitHub Actions
- ホスティングは Vercel

### RLS についての方針（重要）

- **アプリのデータ（articles / purchases など public スキーマ）へのアクセス制御は、アプリケーション層（Server Actions / データ取得層）で書く。** Drizzle は Postgres に直接接続するため RLS の保護対象にならない（接続ロールが RLS をバイパスする）。「Drizzle で書いているから RLS が守ってくれる」という誤解を学習者がしていたら必ず訂正する
- **Supabase Storage のアクセス制御はストレージポリシーで書く**（ステップ9）。こちらはクライアントから Supabase API を直接叩くため、ポリシーが実際の防衛線になる
- public スキーマのテーブルに RLS を「保険として」有効化するかは、Supabase API 経由のアクセス経路が存在するかどうかで判断する。ステップ内で必要になった時点で学習者と議論してよいが、**アプリ層の認可を省く理由には決してしない**

### 使ってよいライブラリ

学習対象外の車輪の再発明を避けるため、以下は導入してよい（導入作業は Claude が行ってよいが、**使う側のコードは学習者が書く**）。

- `zod`（入力バリデーション）
- Markdown レンダラと HTML サニタイズ（`react-markdown` 系など。XSS 対策のサニタイズを省略する構成にしてはならない）
- `@supabase/supabase-js` / `@supabase/ssr`（Auth と Storage 用）
- `stripe` / `resend` / `drizzle-orm` / `drizzle-kit`
- `dayjs`（日付。**`utc` と `timezone` のプラグインを必ず併用する**。素の dayjs の日付演算はランタイムのタイムゾーンに従うので、`Intl` を使わず自前で計算したときとまったく同じずれ方をする。`dayjs.extend()` は 1 か所で済ませ、`Asia/Tokyo` を固定した関数だけを export すること。ゾーンが呼び出しごとの判断になっていると、いつか `.tz()` を書き忘れる）

### 使わないもの

- 状態管理ライブラリ（Redux、Zustand、Jotai など）— RSC ＋ Server Actions ＋ 最小限の `useState` で構成することが学習内容
- UI コンポーネントライブラリ（shadcn/ui、MUI など）
- tRPC、GraphQL、その他の API レイヤー
- Supabase クライアントによる DB テーブルの読み書き（`supabase.from(...)` でのクエリ）— DB アクセスは Drizzle に統一する。Supabase クライアントの用途は Auth と Storage のみ
- `any` を使わない。型が分からない場合は学習者に質問する

### セキュリティ原則（Claude がレビューで必ず見る観点）

学習者のコードを見る機会があれば、指摘の優先度はこの順とする。

1. **認可**: UI で隠すだけでなく、Server Action / Route Handler の入口で毎回セッションと所有権を検証しているか。「他人のリソース ID を直接指定されたら」を常に問う
2. **ペイウォール**: 有料記事の全文がサーバー側で切られているか。クライアントに全文を送って CSS や条件分岐で隠すのは重大な欠陥として扱う
3. **Webhook**: 署名検証をしているか。冪等か（同一イベントの再送で購入が重複しないか）
4. **入力検証**: 外部入力（フォーム、URL パラメータ、アップロードファイルのメタ情報）を zod 等で検証しているか
5. **シークレット**: サーバー専用のキー（Supabase の secret / service キー、Stripe のシークレットキー等）が `NEXT_PUBLIC_` に漏れていないか。クライアントコンポーネントに渡っていないか
6. **Storage ポリシー**: バケットのポリシーが「誰でも何でも書ける」状態になっていないか。パス設計と所有権の対応が取れているか

## ディレクトリ構成

```
src/
  app/              # ルーティング（route group で認証系・閲覧系・ダッシュボードを分けてよい）
  components/       # 共有コンポーネント
  db/               # Drizzle スキーマ・クライアント
  lib/
    supabase/       # Supabase クライアント（browser 用 / server 用）と middleware 補助
    ...             # stripe / resend のクライアント初期化
  actions/          # Server Actions
  types/            # 型定義
e2e/                # Playwright テスト（ステップ10で作成）
drizzle/            # マイグレーション出力
```

単体テストは対象ファイルの隣に `*.test.ts` として置く（コロケーション）。

## 実装ステップ

学習者はこの順で進めます。Claude は**現在のステップより先の実装を提案しない**。各ステップの完了条件をすべて満たしてから次に進む。

---

### フェーズ1: 基盤

#### ステップ1: DB 基盤（Drizzle とスキーマ設計）

- やること: Supabase プロジェクトを作成し、Drizzle を設定して接続する。プロフィールテーブル（profiles: 表示名、アバター画像のパス等。Supabase Auth のユーザー ID を主キーとして参照）と記事テーブル（articles: タイトル、本文、下書き/公開、無料/有料と価格、著者への外部キー、作成/更新日時）を設計する。マイグレーションを生成・適用する
- 使う概念: drizzle.config、スキーマ定義、マイグレーションのライフサイクル、外部キーと制約、**auth スキーマ（Supabase 管理）と public スキーマ（自分が管理）の境界**
- 補足: Supabase Auth のユーザーは `auth.users` にあり、これは Supabase の管理領域なので自分のマイグレーションで触らない。`auth.users` への外部キー参照を Drizzle でどう表現するか（参照する / ID の型だけ合わせて参照しない）はトレードオフがあるので、Claude は選択肢と影響を説明してよい（決定と実装は学習者）
- **完了条件**: `npm run db:migrate` が通り、Supabase のダッシュボードで profiles と articles が確認できる。`npm run db:studio` で閲覧できる。「profiles テーブルがなぜ必要か（auth.users に直接カラムを足さない理由）」を自分の言葉で説明できる

#### ステップ2: 認証（Supabase Auth）

- やること: `@supabase/ssr` でブラウザ用・サーバー用の Supabase クライアントを作り、サインアップ / ログイン / ログアウトを実装する。確認メールのリンクから戻ってセッションが確立するまでのフロー（コールバック処理）を作る。セッション維持のための middleware を設定する。サインアップ完了時に profiles レコードを作成する。ログイン必須ページ（ダッシュボード雛形）を作り、未ログインならリダイレクトする
- 使う概念: Cookie ベースのセッションと SSR の関係（なぜ browser 用と server 用でクライアントの作り方が違うのか）、middleware によるトークンリフレッシュ、認証コールバック、Server Component でのセッション取得とリダイレクト
- 補足: `@supabase/ssr` のクライアント生成コードは公式ドキュメントに雛形があり、バージョン差分も出やすい。Claude はドキュメントの参照先を示してよいが、**写経であっても学習者が自分の手で書き、各行が何をしているか説明できること**をこのステップの目的とする
- **完了条件**: 新規登録 → 確認メール受信 → リンクで有効化 → ログインの一連が通り、profiles にレコードができている。ログアウトが効く。未ログインで `/dashboard` を開くとログインページへ飛ぶ。誤ったパスワードでエラーメッセージが画面に表示される（コンソールではなく）。**ページを開き直してもログイン状態が維持される**

#### ステップ3: 記事の作成・編集・削除（CRUD ＋ 認可）

- やること: 記事の新規作成・編集・削除を Server Actions で作る。本文は Markdown をテキストエリアで入力。下書き/公開、無料/有料と価格を設定できる。ダッシュボードに自分の記事一覧を出す
- 使う概念: Server Actions、zod によるバリデーション、**Server Action 内での認可（セッション確認＋所有権確認をアプリ層で書く）**、フォームのエラー表示、`revalidatePath`
- **認可判定を単体テストしない理由**: 認可はクエリの `where` に `and(eq(articles.id, id), eq(articles.authorId, user.id))` として書く。取得してから比較する形より安全（「存在しない」と「他人のもの」が区別できず ID の存在有無が漏れない。確認と更新の間に記事が消える隙間もできない）だが、判断が SQL に埋まるため Vitest だけでは検証できない。実 DB を使う結合テストはステップ10 と同規模の環境構築になるので、**認可の検証はステップ10 の E2E に寄せる**。それまでの根拠は手動確認とする
- **完了条件**: 作成 → 編集 → 削除が一通り動く。**別ユーザーでログインし、他人の記事 ID を指定した編集・削除リクエストを送ると拒否される**（URL 直打ちや開発者ツールからの実行で確認する）。バリデーションエラー（空タイトル、有料なのに価格 0 など）が画面に表示される。バリデーションに Vitest の単体テストがある。**認可判定は単体テストの対象にしない**（下記）

#### ステップ4: 閲覧とペイウォール

- やること: 公開記事の一覧（新着順）と記事詳細ページを作る。Markdown をレンダリングする。有料記事は未購入者（この時点では全員）に冒頭部分だけ表示し、続きはロック表示にする。**本文の切り出しはサーバー側で行う**
- **このステップの開始時に `cacheComponents: true` を有効にする。** ステップ3 までのページはすべて認証必須で、キャッシュできるものが無いため後回しにした。公開記事の一覧と詳細が最初のキャッシュ対象になる。有効化すると `cookies()` / `headers()` を `<Suspense>` の外で読めなくなり、`<Activity>` により画面遷移してもフォームの入力値と `useActionState` の結果が保持される（送信後の状態を自分でリセットする必要が出る）
- 使う概念: Server Components でのデータ取得、動的ルート、閲覧可否のロジックをデータ取得層に寄せる設計、`notFound()`、`use cache` / `cacheLife` / `cacheTag`
- **完了条件**: 有料記事を開くと冒頭のみ表示される。**ブラウザの開発者ツールでレスポンスを確認し、HTML にもデータにも全文が含まれていない**。抜粋の切り出しに単体テストがある。**閲覧可否判定は単体テストの対象にしない**（この時点の材料は `price` だけで判定が 1 行になり、関数に切り出す価値がステップ6 の購入判定まで出ないため。ステップ6 で判定に「購入済みか」が加わった時点で切り出してテストを書く）。**下書き記事は公開 URL では誰が来ても 404**（`use cache` した詳細ページでは閲覧者を判別できないため、本人向けのプレビューは作らない。作るならダッシュボード配下の別ルートにする）

---

### フェーズ2: 決済

#### ステップ5: Stripe Checkout で購入する

- やること: purchases テーブルを設計してマイグレーションを追加する（既存 DB への追加変更を経験する）。有料記事の詳細に購入ボタンを置き、Server Action で Checkout Session を作ってリダイレクトする。成功/キャンセルの戻り先ページを作る。**購入の確定はこの時点では行わない**（次ステップの Webhook のみを正とする）
- 使う概念: 稼働中スキーマへのマイグレーション追加、Stripe SDK、Checkout Session のパラメータ設計（何を metadata に入れるか）、金額を DB 側の値から取ることの意味（クライアントから金額を受け取らない）
- **完了条件**: テストカードで支払いが完了し、Stripe ダッシュボード（テストモード）に決済が記録される。自分の記事・無料記事・購入済み記事には購入ボタンが出ない。**支払い完了直後の時点では記事はまだ解放されない**（それで正しい）

#### ステップ6: Webhook で購入を確定する

- やること: Stripe Webhook の Route Handler を作る。署名を検証し、`checkout.session.completed` を受けて purchases にレコードを確定する。同一イベントが再送されても重複しないようにする（冪等性）。購入済みならペイウォールを解除して全文を表示する。ローカルでは Stripe CLI（`stripe listen`）で受ける
- 使う概念: Route Handler で raw body を扱う理由、Webhook 署名検証、冪等性の担保（イベント ID の記録または unique 制約）、「決済の真実は Webhook にある」という設計
- **完了条件**: 購入完了後、その記事の全文が読めるようになる。Stripe CLI で同一イベントを再送しても purchases が重複しない。署名が不正なリクエストは 4xx で拒否される。Webhook ハンドラの署名検証と冪等性に結合テストがある

#### ステップ7: レシートメールと購入履歴

- やること: 購入確定時（Webhook 内）に Resend でレシートメールを送る。ダッシュボードに購入履歴ページを作る
- 使う概念: Webhook 内での副作用の順序と失敗時の扱い（メール送信失敗で購入確定が巻き戻ってよいのかを考える）、一覧のためのリレーションを含むクエリ
- **完了条件**: 購入後にレシートメールが届き、記事名と金額が正しい。購入履歴に購入日・記事・金額が表示される。メール送信を意図的に失敗させても（API キーを一時的に壊すなど）購入自体は確定する

#### ステップ8: 売上ダッシュボード

- やること: 自分の記事の売上を集計して表示する（記事ごとの販売数・売上、全体の合計、月別推移）
- 使う概念: Drizzle での集計クエリ（`count` / `sum` / `groupBy`）、集計を DB に寄せるかアプリで計算するかの判断
- **完了条件**: 複数アカウント・複数記事で購入を発生させた状態で、表示される件数・合計が purchases の実データと一致する。**他人の売上は一切見えない**

---

### フェーズ3: 画像

#### ステップ9: Supabase Storage で画像アップロード

- やること: 画像用バケットを作成し、記事のカバー画像とユーザーアバターをアップロードできるようにする。パス設計（例: ユーザー ID をプレフィックスにする）を決め、**ストレージポリシーで「自分の領域にしか書けない」ことを保証する**。クライアントからアップロードし、アップロード前にファイル種別・サイズをアプリ側でも検証する。アップロード後、画像のパスを profiles / articles に保存し、各画面に表示する
- 使う概念: バケットの公開/非公開の選択とその影響、ストレージポリシーの書き方（認証ユーザー・パスと所有者の対応）、クライアント直接アップロードとサーバー経由の比較、`next/image` と外部ドメインの許可設定
- 補足: ストレージポリシーは SQL で書く。ポリシーの構文・利用できる関数はドキュメント確認が必要なので、Claude は公式ドキュメントを参照しながら**構文の説明とレビュー**をしてよい（ポリシーの設計と記述は学習者）。バケット作成などダッシュボード操作の案内は具体的にしてよい
- **完了条件**: カバー画像を設定すると一覧と詳細に表示される。アバターが表示される。サイズ超過（例: 10MB 超）や画像以外のファイルが拒否される。**他人のユーザー ID のパスへのアップロードがストレージポリシーで拒否されることを、実際にリクエストを作って確認した**。ポリシーの各行が何を許可・拒否しているかを説明できる

---

### フェーズ4: 品質と公開

#### ステップ10: E2E テスト（Playwright）

- やること: Playwright の環境構築は Claude が行う。学習者は主要フローの E2E を書く。(1) サインアップ〜ログイン、(2) 記事作成〜公開〜一覧表示、(3) 有料記事のペイウォール表示、(4) **他人の記事の編集・削除が拒否される**（ステップ3 から持ち越した認可の検証）。購入フローの E2E は Stripe のテスト環境が絡み難度が高いため任意（挑戦する場合は Claude が構成を相談に乗る）
- 使う概念: E2E の対象選定（すべてを E2E にしない）、テストデータの独立性（実行のたびに新しいユーザーを作る等）、確認メールを挟む認証フローをテストでどう扱うか（Claude は選択肢の比較を説明してよい）、セレクタの安定性
- **完了条件**: `npm run test:e2e` がローカルで 2 回連続成功する。テスト間に依存がない（単体で 1 本だけ実行しても通る）

#### ステップ11: CI（GitHub Actions）

- やること: ワークフローの雛形は Claude が用意してよいが、**各ジョブが何をしているかを学習者が説明できること**を完了の条件とする。PR で lint / typecheck / unit test / build を実行する。E2E の CI 実行は任意
- 使う概念: CI で落ちる=マージできないという開発フロー、環境変数とシークレットの扱い（CI にダミー値が必要なもの）
- **完了条件**: PR を作るとチェックが走る。わざと型エラーを入れた PR でチェックが落ち、直すと通る。学習者が各ジョブの内容を説明できる

#### ステップ12: Vercel デプロイ

- やること: Vercel に本番デプロイする。環境変数を設定し、Stripe の本番用 Webhook エンドポイント（テストモード）を登録する。Supabase Auth のリダイレクト URL 設定（Site URL / Redirect URLs）を本番 URL に合わせる。本番 URL で一連の動作を確認する
- 使う概念: 環境ごとの設定の分離、ビルド時と実行時の環境変数、本番 Webhook の登録と `STRIPE_WEBHOOK_SECRET` の環境差分、認証コールバック URL の環境差分
- **完了条件**: 本番 URL でサインアップ（確認メール含む）→ 記事投稿 → 画像アップロード → テストカードでの購入 → 全文解放までが通る。ローカルの `stripe listen` なしで本番 Webhook が受信できている

---

## 機能追加フェーズ（完成後）

ステップ12 まで完了したら、実務の機能追加・改修と同じ形式で続ける。進め方:

1. 学習者がお題を選び、**要件を issue 形式（背景 / 要件 / 受け入れ条件）で書く**
2. Claude は要件のレビュー・設計相談・影響範囲の指摘を行う（実装はしない）
3. 実装後、Claude がセキュリティ原則の観点でレビューする

お題の候補（難度順ではない。学習者が選ぶ）:

- クリエイターごとの月額メンバーシップ（Stripe サブスクリプション。単品購入と共存させる設計判断が本題）
- Stripe Connect によるクリエイターへの売上分配（オンボーディング、手数料設計）
- コメント・スキ（いいね）機能
- フォローとフォロー中フィード
- 記事検索（Postgres の全文検索）
- 管理者ロールの導入と通報対応（単一ロールから多ロールへの移行という実務的な改修）
- 下書きの共有リンク（トークン付き URL での限定公開）
- 有料記事内の画像保護（非公開バケット＋署名付き URL への移行。公開バケットからの実務的な引っ越し）
- 画像のリサイズ・最適化
- Server Action へのレートリミット
- Google ログインの追加（Supabase Auth のソーシャルログイン）
- 予約公開（公開日時指定）

## 詰まりやすいポイント

- Client Component に非同期処理や DB アクセスを書こうとして詰まる。着眼点は「この処理はどこで実行されるべきか。ブラウザに渡ってよい情報か」。境界（`"use client"`）の位置を疑う
- Server Action が UI から呼べているので安心してしまう。着眼点は「この Action は UI を経由せず直接呼べる。その場合に何が起きるか」。認可は Action の入口で毎回行う
- 「Drizzle で書いているから RLS が守ってくれる」と考えてしまう。着眼点は「この接続はどのロールで、RLS の適用対象か」。アプリ層の認可が唯一の防衛線であることを思い出す
- ペイウォールを「クライアントで出し分ければよい」と考えてしまう。着眼点は「ネットワークタブに見えるレスポンスに何が含まれているか」。データを渡した時点で公開したのと同じ
- ログインしたはずなのにサーバー側でセッションが取れない・いつの間にかログアウトしている。着眼点は「セッションは何に保存され、誰がいつ更新しているか」。middleware の設定と Cookie の流れを確認する
- 確認メールのリンクを踏んでもログイン状態にならない。着眼点は「リンクから戻った後、コードをセッションに交換する処理はどこにあるか。リダイレクト URL は Supabase 側の許可リストにあるか」
- 一覧が更新されない・し過ぎる。着眼点は「このページはいつ再レンダリングされるべきか」。Next.js のキャッシュと再検証の挙動はバージョン差分が大きいので、**推測ではなく現行バージョンのドキュメントで確認する**（Claude も同様）
- Webhook で `request.json()` を使って署名検証に失敗する。着眼点は「署名は何に対して計算されているか」
- **決済は成功するのに Webhook が一度も届かない。** 着眼点は「アプリのキーと `stripe listen` は同じアカウントを見ているか」。エラーも警告も出ないので、`stripe login list` と `accounts.retrieve()` で照合するまで気づけない。詳細は「外部サービスの準備」の節
- Webhook の再送で購入が二重になる。着眼点は「同じイベントが 2 回届いたとき、2 回目は何を根拠に無視するか」
- 購入直後に成功ページを開いても記事が解放されていない。着眼点は「確定させるのはリダイレクトか Webhook か。両方で確定させるとどうなるか」。成功ページ側は「反映待ち」を表現するのが正攻法
- 金額をフォームやクエリパラメータから受け取ってしまう。金額の正は常に DB
- Storage へのアップロードがポリシー違反で失敗する。着眼点は「このリクエストは誰として実行されているか。ポリシーはそのパスへの書き込みを誰に許可しているか」
- 環境変数が `undefined` になる。着眼点は「その変数はサーバー用かクライアント用か。`NEXT_PUBLIC_` の意味は何か。dev サーバーを再起動したか」
- E2E が 2 回目の実行で落ちる。着眼点は「前回の実行が残したデータに依存していないか」

## 作業メモ（随時更新する）

セッションをまたいで作業を再開するための覚え書き。進捗が変わったら書き換える。

### 現在地

**ステップ9 完了（9-9 アバターは保留課題1 に移した）。次はステップ10（Playwright の E2E）。**

ステップ9 の完了条件のうち「アバターが表示される」だけ満たしていない。**ステップ12 の本番確認でも出てこない項目**なので、機能追加フェーズではなくプロフィール設定画面と一緒にやる。


### ステップ9 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 9-1 | バケットの作成と公開設定の確認 | 完了（`images` / 公開 / 10MB / jpeg・png・webp） |
| 9-2 | ストレージポリシー（INSERT / SELECT / DELETE） | 完了（本番・ローカルとも適用済み） |
| 9-3 | ブラウザからのアップロード疎通 | 完了（`/storage-test` は 9-6 で消す） |
| 9-4 | ファイルの検証（種別・サイズ）＋ 単体テスト | 完了（変異 4 種すべて検知） |
| 9-5 | `articles` にカバー画像のパスを持たせる | 完了（check 制約 ＋ DB テスト。変異 3 種すべて検知） |
| 9-6-1 | フォームに画像の選択欄を置き、選んだ時点で検証する | 完了 |
| 9-6-2 | `articleFormSchema` に `coverImagePath` を足す ＋ テスト | 完了（変異 9 種すべて検知） |
| 9-6-3 | Action と DAL に `coverImagePath` を通す | 完了 |
| 9-6-4 | 送信時にアップロードして、実際のパスを送る | 完了（ポリシーに弾かれる側も確認済み） |
| 9-6-5 | 画像を選ばずに保存したとき、既存の画像を保つ | 完了 |
| 9-6-6 | 差し替えたとき、古いファイルを消す | 完了 |
| 9-7 | 一覧・詳細に表示する（`next/image`）＋ 編集画面のサムネイル | 完了 |
| 9-8 | 他人のパスへの書き込みが拒否されることを確認する | 完了（6 件とも期待どおり） |
| 9-9 | アバター | **保留課題1 に移した**（2026-09-15 の判断） |

ステップ9 で決めたこと:

- **バケットは `images` 1 つ。公開。** パスは `{userId}/covers/{nanoid}.{jpg|png|webp}`。10MB 上限と MIME 制限（jpeg / png / webp）をバケット側にも設定してある。**SVG は入れない**（XML なので `<script>` を書ける）
- **公開バケットの public URL はポリシーを通らない。** CDN が直接返すので、未ログインでも開ける。SELECT ポリシーが効くのは API 経由の読み取り（`list` など）だけ。「読めてしまった」を見てポリシーを疑わないこと
- **UPDATE のポリシーは作らない。** 上書きせず、毎回新しいパスに置いて古いものを DELETE する方針にしたため。キャッシュを考えなくて済み、DB の更新が唯一の確定点になる。UPDATE を使うようになったら `using` と `with check` の**両方**が要る（片方だと他人の領域へパスを移せる）
- **ポリシーは `supabase/policies/storage.sql`。`db:migrate` には入れない。** `storage.objects` の持ち主は `supabase_storage_admin` で、アプリがつなぐ `postgres` はそのメンバーではない（`can_manage: false` を実測）。**ローカルの Supabase では `postgres` が superuser なので通ってしまい、「ローカルでは成功して本番だけ落ちる」差になる。** 適用は本番・ローカルとも SQL Editor から手で行う。手順は README
- **ファイルの検証（`src/lib/validate-image-file.ts`）は防衛線ではない。** `File.type` はブラウザの申告値で詐称できる。止めているのはバケットの設定とポリシー。この関数の役目は、10MB を送りつけて待たされた末に失敗する体験を避けること
- **許可リストと拡張子は同じ 1 つの表から出す。** `Record<string, string>` にして引き当て、`undefined` なら不許可。「メンバーシップの確認」と「引き当て」を別の行に分けると、表を直したときにずれる。`Object.keys().includes()` は**型を絞らない**ので、引き当ててから `undefined` を見る形にした
- **カバー画像のパスは URL ではなくパスだけを保存する。** URL の前半は環境の情報で、プロジェクトを作り直すと全行が壊れる。保留課題（非公開バケット＋署名付き URL への移行）では URL は毎回作るものになる
- **check 制約 `cover_image_in_author_folder` で「パスの先頭フォルダが著者の ID」を固定した。** ポリシーの `(storage.foldername(name))[1] = auth.uid()::text` と同じ規則を、同じ形（`split_part(path, '/', 1) = author_id::text`）で書いてある。**2 つの防衛線が同じ言葉で書かれている**
- **「含まれる」では弱い。** `{他人のID}/covers/{自分のID}.jpg` が通ってしまう。**区切りの `/` も忘れない**。無いと `{自分のID}xx/...` が前方一致で通る。どちらも実測で確認済み
- **check 制約は式が NULL になった行を「満たしている」とみなす。** `WHERE` とは逆。カバー画像なし（NULL）は `IS NULL OR` を書かなくても通る
- **エラーの絞り込みは `assert`（Vitest）。** 型定義が `asserts expression` なので、実行時の検査と型の絞り込みを同時にやる。`expect(...).toBeInstanceOf(...)` は**型を絞らない**。`.catch((e: unknown) => e)` の注釈も要る（付けないと `any` のまま `.code` が素通りする）
- **ファイル入力に `name` を付けない。** 付けると選択したファイル本体が FormData に載り、Server Action のボディ上限（既定 1MB。`action-handler.js` で確認）で 413 になる。**実際に踏んだ。** 上限を上げても Vercel の 4.5MB があるので、10MB を許す以上クライアント直送しか成立しない。だから認可はアプリのコードではなくストレージポリシーが担う
- **`useActionState` に渡す関数をクライアント側のラッパーにする。** `(prevState, formData) => Promise<ArticleFormState>` という契約は Server Action と同じなので、中でアップロードしてから `formData.set()` して Server Action を呼べる。`isPending` と `state` はそのまま効く。**hidden にアップロード後のパスを入れる形は成立しない**（FormData が組み立てられるのは action が走る前）
- **失敗しても書きかけを捨てない。** セッション切れで `redirect()` すると本文が消える。ラッパーも `getString(formData, ...)` で送信された値を組み立てて返す。**props の `defaultValues` を返すと入力が初期値に戻る**
- **編集時の既存パスは hidden で送り返す。`defaultValue` は props ではなく `state` から取る。** 保存に失敗したとき、直前に上げた画像のパスが戻ってくるので、上げ直して孤児を増やさずに済む
- **クライアントから来るパスを止めているものは 2 つ。** zod は**形だけ**（`{uuid}/covers/{nanoid}.{ext}`）、check 制約が**誰のものか**。他人の UUID は形が正しいので zod を通り、DB で落ちて `error.tsx` に飛ぶ。ストレージポリシーはここには関与しない（守るのはファイルの読み書き）
- **`<input type="file">` に値は入れられない**（ブラウザが禁止している）。編集画面で「選択されていません」と出るのは正しい。現在の画像は別に表示するしかない
- **テストデータの中でも、壊す規則は 1 つだけ。** パスは 4 つの部分（先頭・`covers`・ファイル名・拡張子）でできていて、それぞれが独立した規則。`user-id/other/...` のように 2 つ壊すと、ラベルが名指ししている規則を壊してもテストが落ちない（実測で 3 つの変異が素通りした）
- **公開 URL は自前の純粋関数で組み立てる**（`src/lib/cover-image-url.ts`）。Supabase の `getPublicUrl()` はクライアントオブジェクトが要り、サーバー用の `createClient()` は `cookies()` を読む。記事の一覧と詳細は `use cache` なので**それだけで壊れる**。空文字も `null` にするのは、編集フォームの「画像なし」が `""` だから（DB 由来の 2 か所は `null`、フォーム由来の 1 か所だけ `""`）
- **`remotePatterns` はホストとパスの両方を絞る。** ホスト名は `NEXT_PUBLIC_SUPABASE_URL` から作る（ベタ書きするとリポジトリが 1 つの Supabase プロジェクトに固定される）。**ステップ11 の CI では `next.config.ts` を読む時点で落ちるので、build するジョブにこの変数が要る**
- **画像の実寸を保存していないので、枠の比率を固定して `object-cover` で埋める。** `width` / `height` の固定値だと縦長の画像が潰れる。`sizes` は表示幅に合わせる（詳細 672px / 一覧のサムネイル 96px）。`priority` は詳細だけ（一覧の 20 件に付けると意味が無くなる）
- **カバー画像の `alt` は空文字。** すぐ上の `<h1>` が同じ情報を伝えているので、タイトルを入れると 2 回読み上げられる。**「必ず埋める」ではなく「その画像が伝えている情報が他にあるか」で決める**
- **ストレージポリシーを直接リクエストで確認した**（2026-09-15、ローカルの Supabase。本番はポリシーの定義が同一であることを `pg_policies` で確認）。6 件とも期待どおり
  - A が自分のフォルダに上げる → **成功**（これが無いと「届いていないだけ」と区別できない）
  - A が B のフォルダに上げる / 未ログインで上げる → `new row violates row-level security policy`
  - **A が B のファイルを `remove` → `error` は `null`。** 消した件数 0 でファイルは残る。**エラーだけ見ると「削除成功」と読み違える**。`USING` で見えない行は削除の対象にすらならない
  - A が B のフォルダを API で一覧 → 0 件
  - 公開 URL は未ログインでも 200。**これは正しい動作**（CDN が返すのでポリシーを通らない）
- **DB の check 制約のテストは `src/db/schema.db.test.ts`。** 対象が関数ではなくスキーマなので DAL の隣ではない。フィクスチャは `Partial<$inferInsert>` を受け取る形だったのでそのまま使えた

### ステップ8 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 8-1 | 記事ごとの販売数・売上（DAL ＋ DB テスト） | 完了（変異 7 種すべて検知） |
| 8-2 | 全体の合計をどこで出すか | 完了（`sumSales`。アプリ側で合計する） |
| 8-3 | 月別推移（DAL ＋ 0 埋めの純粋関数） | 完了（`getMySalesByMonth` は `range` を引数で受け取る） |
| 8-4 | 売上ページ | 完了 |
| 8-5 | ダッシュボード配下の共通ナビ | 完了 |

ステップ8 で決めたこと:

- **`purchases` に `sellerId` は足さない。** 集計は `articles` を join して `authorId` で絞れば足りる。保留課題の「ステップ8 で欲しくなったら判断する」への答えはこれ。非正規化を 1 つ回避した
- **`count(*)` ではなく `count(purchases.id)`。** `leftJoin` と `count(*)` を組み合わせると、売れていない記事が NULL の行 1 つを数えて **1 件**になる。列を指定した `count` は NULL を数えない
- **`sum()` は `numeric` を返すので Drizzle では `string | null`。** `coalesce(sum(...), 0)` ＋ `.mapWith(Number)` で型と値の両方をそろえる
- **起点のテーブルが違うと、同じ条件でも意味が変わる。** 記事ごとは `articles` 起点の `leftJoin`（売れていない記事も 0 件で出す）、月別は `purchases` 起点の `innerJoin`。`isNotNull(articles.publishedAt)` は**記事ごとにだけ効く**（月別では購入の無い記事がそもそも出てこないので何も除外しない）。同じ名前のテストをコピーしても意味は移らない
- **「購入がある記事の `publishedAt` は必ず入っている」は 3 つに分かれて支えられている**（`getArticleForCheckout` の `status = 'published'` / check 制約 `published_requires_date` / `updateMyArticle` が `publishedAt` を消さない）。**他の関数にコメントで書いても守れない**（変更する人はそのファイルを読まない）。固定するなら保留課題10 のテスト
- **全体の合計はアプリ側で足す。** 一覧を出さずに合計だけ欲しい画面が無く、行は既に手元にある。純粋関数にできるので DB 不要のテストで固定できる
- **集計期間は直近 12 か月の固定。起点は引数で受け取る**（既定値 `new Date()`）。DB の `now()` に頼るとフィクスチャが年が変わった瞬間に窓から外れる。期間選択は保留課題9
- **日付は dayjs ＋ `utc` / `timezone` プラグイン。`src/lib/dayjs.ts` が `jst()` だけを export する。** 素の `dayjs()` も `new Date(y, m, d)` も `getMonth()` も**ランタイムのタイムゾーン**に従う。**Vercel と GitHub Actions は UTC、手元は JST。** 同じ入力で窓が 1 か月ずれる（実測）。ゾーンを呼び出しごとの判断にすると必ず忘れる
- **月は `'YYYY-MM'` の文字列で扱う。** `Date` で返すと表示のときに再変換が挟まって月がずれる。`salesMonthRange` が 12 個のラベルを返し、SQL 側は `inArray` で 1 条件になる（`gte` / `lt` の 2 境界だと、上限が排他か包含かで必ず 1 か月ずれる）。**同じ配列が 0 埋めの枠にもなる**ので、窓と枠がずれようがない
- **タイムゾーンのテストは上下の境界 1 件ずつで足りる**（実測。`AT TIME ZONE` を外すとその 2 件だけが落ちる）。JST の月初直後（`2025-05-01T05:00+09:00`）と、最終月の直後（`2026-05-01T01:00+09:00`）。どちらも UTC で丸めると別の月に入る値を選ぶ
- **年またぎや月末のクランプはテストしない。** dayjs の月演算の振る舞いであって、こちらの規則ではない。テストするとライブラリの写経になる
- **テストがタイムゾーンに依存していないことは `TZ=UTC` で実行して確かめる。** 東京時間の日中に実行している限り、この種のずれは絶対に露見しない
- **`jst()` が固定しているのは「出力」であって「入力の解釈」ではない**（2026-09-10 に実測）。`dayjs(input).tz()` は 2 段階で、`dayjs(input)` が瞬間を決め（**オフセットの無い文字列はローカル TZ**）、`.tz()` はその瞬間を JST の壁時計で表すだけ。瞬間は変えない。`jst("2026-01")` は UTC+14 の環境で **2025年12月31日**になる。引数が `Date` なら瞬間が確定しているので安全（現在の呼び出しは 2 か所とも `Date`）。入力の側からゾーンを決めたいときは `dayjs.tz(input, zone)` という別の API
- **月ラベル（`'YYYY-MM'`）を日付の道具に戻さない。** 表示のために `dayjs` に食わせると、いったん瞬間に変換することになり、文字列で持つと決めたときに消したはずのタイムゾーンの問題が復活する。整形するなら文字列として組み立てる。当面は `2026-01` のまま表示する
- **集計期間は呼び出し側が `range` として渡す。** DAL の中で `salesMonthRange` を呼ぶと、0 埋めの枠と窓が「同じ `baseDate` だから一致するはず」という論証に頼ることになる。ページで 1 回だけ作って両方に渡せば、**同じ配列であることが形で保証される**。DAL から日付の計算も消える。あわせて DB テストから期間の規則（12 か月ぶん・基準月の翌月は含まない）を追い出し、`sales-month-range.test.ts` に一本化した（13 件 → 9 件）
- **ページを足したら `npm run build` まで流す。** `cacheComponents` が有効なので、`new Date()` のような**実行のたびに変わる値**は静的シェルのプリレンダリングで落ちる。売上ページがこれで壊れていた（`getMySales()` の中の `cookies()` より**前**に `new Date()` を呼んでいた）。`await connection()`（`next/server`）を先に置いて「ここから先はリクエスト時に描画する」と宣言する。**typecheck / lint / test はすべて通るので、この種の失敗は build でしか出ない**
- **ダッシュボードの共通ナビは `(requires-profile)/layout.tsx` に置く。** `dashboard/layout.tsx` は `/dashboard/profile/new` も包むので、プロフィール未作成の人に「押すとリダイレクトされるだけのリンク」を見せることになる
- **レイアウトはサーバー側で現在のパスを知れない。** `usePathname` は Client Component 専用。ナビだけを `"use client"` にするのが素直（middleware でヘッダに載せる手もあるが、全リクエストを通る `proxy.ts` に手が入る）。現在地は `aria-current="page"` と、下線と文字色の**両方**で示す
- **売上ページの合計は、画面に出している行（0 埋め済みの 12 か月）から出す。** DAL の生の結果から出しても値は同じだが、「表示を足したら合計になる」が形で保証されない。**記事ごとは全期間、月別は直近 12 か月**なので、合計の見出しに期間を書かないと誤解される

### ステップ7 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 7-1 | Resend の準備と疎通 | 完了 |
| 7-2 | レシートの宛先をどこから取るか | 完了（`metadata.buyerEmail`） |
| 7-3 | `parseSession` から `buyerEmail` を取り出す | 完了（欠けても購入は確定させる） |
| 7-4 | 記事タイトルの取得 | 完了（`metadata.articleTitle`） |
| 7-5 | レシート本文の組み立て ＋ テスト | 完了（`build-receipt.test.ts` 5 件。変異 4 種すべて検知） |
| 7-6 | Webhook から送信 | 完了（API キーを壊しても購入は確定。再送でも 1 通） |
| 7-7 | 購入履歴の DAL ＋ DB テスト | 完了（変異 3 種すべて検知。`where` の欠落は 1 件で捕まる） |
| 7-8 | 購入履歴ページ | 完了 |
| 7-9 | ダッシュボード ⇔ 購入履歴の導線 | 完了 |

ステップ7 で決めたこと:

- **レシートの宛先は `metadata.buyerEmail`。** Checkout の画面で購入者が入力する `customer_details.email` は、入力ミスも他人のアドレスも入りうる。Supabase の Admin API で引き直す案は HTTP 往復が 1 回増える。`metadata` はこちらのサーバーしか書けないので、`buyerId` / `articleId` を信じている根拠がそのまま使える。引き換えに**購入時点のアドレス**に届く（購入後に変更しても追随しない）
- **レシートの材料は、購入の確定を止める理由にならない。** `buyerId` / `articleId` が欠けたら購入行を作れないが、`buyerEmail` / `articleTitle` は `purchases` のどの列にも入らない。必須にすると、**変更前に作られた Session が支払い後に届いたとき**（デプロイの窓）に 2xx で捨てられ、「お金を受け取って何も渡さない」状態になる。`ParseResult` では `string | null` で表し、送信側が分岐しないとコンパイルが通らない形にした
- **入口の `createCheckout` でも `user.email` の欠落で決済を止めない。** 一度は `throw` する形にしたが、Webhook 側が「メールが無くても購入は確定させる」と決めた以上、入口だけ止めるのは矛盾する
- **記事タイトルも `metadata` に載せる。** `line_items` はイベントに含まれず、`listLineItems` は往復が増え、`articles` の引き直しは現在の値になる。金額を `amount_total` にしたのと同じ理由で、**レシートは購入時点の記録**にそろえる
- **`from` は `buildReceipt` の中に置く。** `subject` と本文が同じ関数にあるのに `from` だけ外にあると、文面を変えるのに 2 ファイル開くことになる。戻り値の型は 4 項目の自作の型にして、`resend.emails.send()` に渡す行に `CreateEmailOptions` との互換性を検査させる（`CreateEmailOptions` をそのまま返すと `attachments` などまで許す広い型になる）
- **送信の失敗は 2xx で受理する。** 5xx で再送させても `createPurchase` が `undefined` を返すのでメールは送られない。再送に意味がないので受理し、`error.name` と `error.message` を Event ID / Session ID と併せてログに残す。**中身を捨てると「resend error」しか残らず、キーの問題かレート制限かが分からない**
- **送信元は `onboarding@resend.dev`。** ドメイン未検証の間はこれ以外を送信元にできず、宛先も Resend アカウントの登録アドレスに限られる。ステップ12 の本番でも同じ
- **トップレベルで `requireEnv()` を呼ぶファイルを足したら `vitest.config.mts` の `env` にも足す。** `src/lib/resend.ts` を作ったとき、`route.ts` 経由で import している `route.test.ts` が**丸ごと 0 件になった**（import 時に例外なのでテストが 1 件も実行されない）。落ちたことには気づけるが、緑の件数だけ見ていると見落とす
- **購入履歴は `status` で絞らない。** 履歴の責務は「買ったものを列挙する」こと。リンク先が読めるかどうかは詳細ページ側の責務で、絞ると状態を増やすたびにここを直す必要が出る（保留課題7 と地続き）
- **履歴の金額は `purchases.paymentAmount`。** `articles.price` を引くと、著者が値上げした瞬間に過去の購入者の履歴が本人の払っていない額に書き換わる
- **DAL の戻り値に `buyerId` を足さない。** テストで「自分の行か」を判定したくなるが、返すと**呼び出し側が検算する**書き方の余地ができ、認可を `where` に埋めた判断と逆行する。テスト側は他人の購入を別の記事にして、返っている列だけで識別する
- **`where` に埋まった認可を、初めて自動テストで固定した**（`my-purchases.db.test.ts`）。`where` を消すと 1 件だけ落ちる。ステップ3 から E2E に持ち越していたもののうち、購入履歴ぶんはここで解消した
- **import の member 順は `sort-imports`（`ignoreDeclarationSort: true`）で揃える。** `import/order` は宣言の並びしか見ない。`sort-imports` の宣言側の並び替えは**自動修正できない**ので有効にしない

### ステップ6 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 6-1 | `stripe listen` と Route Handler の疎通 | 完了 |
| 6-2 | 署名検証 ＋ テスト | 完了（`route.test.ts` 5 件。変異 4 種すべて検知） |
| 6-3 | `checkout.session.completed` から purchases に 1 行作る | 完了（本物の購入で確認済み） |
| 6-4 | 冪等性と、失敗時に何を返すか ＋ テスト | 完了（`stripe events resend` で重複しないことを確認済み） |
| 6-5 / 6-6 | ペイウォールの解除 | 完了（データ取得層は不要と判明したため統合。下記） |

ステップ6 で決めたこと:

- **署名は「Stripe が送った」ことしか証明しない。** `metadata` を信じてよい根拠は、この sandbox で Session を作れるのがこちらのサーバーだけで、そこが `requireUser()` と `canPurchase` を通っているから。ダッシュボードや `stripe trigger` が作った Session には `metadata` が無いので、欠けている場合は必ず扱う
- **応答コードは失敗の種類で分ける。** Stripe は 2xx 以外を受け取ると再送する（4xx でも再送される）。一時的な失敗（DB の接続断）は投げっぱなしで 5xx にして再送させ、再送しても直らない失敗（`metadata` が無い、記事が削除済み）は 2xx で受理する。**2xx は「処理した」ではなく「もう送ってこなくていい」なので、必ずログに残す**
- **冪等性は「行を重複させない」だけでは足りない。** 副作用も 1 回でなければならない。`ON CONFLICT DO NOTHING` ＋ `returning()` で「今このイベントで作れたのか」を区別できるようにしてある。ステップ7 のレシートメールはこの戻り値で送信を判断する
- **金額の正は `session.amount_total`。** `metadata` に入れた値でも `articles.price` の引き直しでもない。引き直すと、値上げ後に「買った人が払っていない額」を記録する
- **Stripe はイベントの順序を保証しない。** 実際のログで `payment_intent.created` が `payment_intent.succeeded` より後に届いた。1 イベントだけを見て判断する形にしておく
- **`stripe listen` は既定で全イベントを転送する。** 対応していない種別も 2xx で受理しないと、3 日間再送され続ける。ステップ12 で本番エンドポイントを登録するときは送るイベントを絞れる
- **署名は受け取ったバイト列そのものに対して計算されている。** `req.json()` で受けて組み立て直すと壊れる。ただし `JSON.stringify` の出力をそのまま送るテストでは差が出ないため、**インデント付きの本文を送るテスト**でこの 1 点を固定している
- **テストの分担**: 署名検証は `route.test.ts`（DB 不要。未対応の種別を使って `createPurchase` に到達させない）、取り出しの判断は `parse-session.test.ts`（純粋関数）、冪等性は `stripe-webhook.db.test.ts`
- **DB テストの読み戻しは `src/test/fixtures.ts` に置く。** DAL に本番から呼ばれない関数を入れると「アプリが DB に触る唯一の入口」という性質が薄まる。値の検証は**入力と比べる**。`RETURNING` の結果と読み戻した行はどちらも DB 由来なので、両者を比べても何も確かめられない
- **ペイウォールの解除に専用のデータ取得層は要らなかった。** `getPublishedArticle` が既に本文全体を返している。キャッシュされるのは記事 ID ごとのクエリ結果で、閲覧者では変わらない。**誰に何を見せるかは描画のときに決まる**ので、本文の領域を独立した `<Suspense>` に切り出すだけで足りる。`fallback` には抜粋を置く（誰が見ても同じで、キャッシュ済みのデータからすぐ作れる）
- **`canViewFullBody` は `!canPurchase(...)` で表す。** 現状の規則では完全な補集合で、2 つ持つと同じ規則が 2 か所に書かれる。**呼び出し側で名前を付けて意図を残す**こと。保留課題7（販売停止）を入れると一致しなくなるので、そのとき分ける
- **`canPurchase` の `userId` は `string | null`。** 未ログインの閲覧者に対する答えは true（ログインすれば買える）。偽の ID を渡すより状態をそのまま型で表す。おかげで未ログインの分岐が画面から消えた
- **認証スタブは 3 状態を判別可能なユニオンで持つ**（`src/test/current-user-stub.ts`）。「未指定」と「意図的に未ログイン」を `null` 一つで表すと、`signInAs()` の書き忘れが未ログインのテストとして静かに通る。フラグを別変数にすると同期の責任が生まれ、意味の無い組み合わせが書けてしまう
- **React の `cache()` は Vitest では畳み込まない**（実測）。リクエストのスコープに紐づく仕組みなので、包んだ側をそのままテストしてよい。包む前の関数を export すると `cache()` を迂回する口が残る
- **公開ページのヘッダーは、アカウントのリンクだけを `<Suspense>` に入れる。** ヘッダー全体を閲覧者依存にすると記事一覧や本文が認証の往復を待つ。`/` は `◐` になるが、静的シェルに記事一覧は残る
- **自己購入を DB で防いでいない**（2026-09-06 に確認）。CHECK は同じ行の列しか見られず、`purchases` に著者の ID が無いため書けない。防ぐなら `articles` に `UNIQUE (id, author_id)` を張り、`purchases` に `sellerId` を足して複合外部キー ＋ `CHECK (buyer_id <> seller_id)`。通常の経路では `createCheckout` の `canPurchase` が防いでいる。**ステップ8 の集計で `sellerId` が欲しくなったら、同じ変更で両方満たせるのでそのとき判断する**。なお Webhook で再チェックしてはいけない（支払い済みなので、拒否すると「お金を受け取って何も渡さない」状態になる）

### ステップ5 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 5-1 | purchases テーブルの設計とマイグレーション | 完了 |
| 5-2 | Stripe の準備（テストキー・Stripe CLI・環境変数） | 完了 |
| 5-3 | 購入ボタンの表示判定 | 完了（`canPurchase` は変異 6 種中 5 種を検知。残る 1 種は負の価格で、DB の check 制約が防ぐ） |
| 5-4 | Checkout Session を作る Server Action | 完了（テストカードで Stripe の決済画面まで確認済み） |
| 5-5 | 成功 / キャンセルの戻り先ページ | 完了（`stripe listen` 無しで諦める画面まで確認済み） |
| 5-6 | 購入された記事を削除しようとしたときの扱い | 完了 |

ステップ5 で決めたこと:

- **金額の正は DB。** フォームから来るのは `articleId` だけで、`unit_amount` は Action 内で引き直した `articles.price` を使う
- **`metadata` は Checkout Session のトップレベルに置く。** `price_data.product_data.metadata` は Product に付くもので、`checkout.session.completed` のペイロードには現れない（`line_items` 自体が含まれないため）。ステップ6 で買い手と記事を特定できなくなる
- **`managed_payments: { enabled: false }` は省略できない**（省略するとエラーになる）。既定値を明示しているように見えるのでコメントを残してある
- **`APP_URL` に `NEXT_PUBLIC_` は付けない。** `success_url` / `cancel_url` の組み立てにしか使わず、ブラウザでは読まない
- **`canPurchase` は純粋関数**（`src/app/articles/[id]/can-purchase.ts`）。購入済みかを引数で受け取る。DB アクセスと混ぜるとモックだらけになり実装の写経になるため
- **Paywall は独立した `<Suspense>` に置く。** 記事本文と同じ境界に入れると、本文の表示が `getCurrentUser()` の HTTP 往復と購入クエリを待ち、`use cache` がヒットしても毎回止まる
- **DAL のファイルは用途でも括る。** `src/lib/dal/checkout.ts` は「Checkout を開始してよいか判断するための材料」を集める。ここの記事クエリは**キャッシュしてはいけない**（金額の正を取りに行くので、古い値だと値上げ直後に旧価格で決済される）
- **記事 ID に zod を掛けていない。** 引き当てそのものが検証を兼ねており（`updateMyArticle` / `deleteMyArticle` は所有権まで同時に確認する）、形式チェックを前に置いても強くならない。不正な ID は行が 0 件になって `notFound()` に落ちる。判断の根拠は「外部入力を**根拠に判断していない**」こと。`articleFormSchema` の対象は保存される値なので事情が違う
- **DAL の関数から `userId` の引数を無くす。** 呼び出し側が渡す形は、渡し間違えれば他人のデータが読める。呼び出し元が全員「現在のユーザー」を渡しているなら、関数側で `requireUser()` を呼べば危険な形そのものが消える（`src/lib/dal/my-purchases.ts`）。名前で警告し続けるより確実
- **クライアントコンポーネントから DAL を import しない。** `requireUser()` → `@/lib/supabase/server` → `next/headers` と連鎖して、`Error: You're importing a module that depends on "next/headers"` で落ちる。クライアントから呼べるのは `"use server"` の Server Action だけ。ここで止まらなかったら DB クライアントと Supabase の鍵がブラウザに送られていた。恒久的に塞ぐなら `server-only` パッケージ（未導入）
- **決済後の戻り先は購入を確定させない。** Stripe のリダイレクトと Webhook は独立した経路で、順序は決まっていない（リダイレクトが先に着くことが多い）。しかもブラウザを閉じた人にはリダイレクトが来ないので、Webhook は必ず要る。要る経路が 1 本あるなら 2 本目は複雑さを足すだけ
- **`<Suspense>` では Webhook を待てない。** 待てるのは「そのリクエストの応答を返すまで」だけ。Webhook はレスポンスを返した後に届くので、リクエストをまたぐ待ち（クライアントからのポーリング）が要る。`/success` は 2 秒間隔で 10 回まで確認する
- **反映されないときに「失敗しました」と書かない。** 支払いは成立している。失敗しているのはこちらの反映。支払いを受け付けたことを明示し、`session_id` を添えた問い合わせ導線を出す
- **`payment_status` は `=== "paid"` で判定する。** Stripe の型は `OtherString` を含むので、`!== "unpaid"` だと将来増えた値を支払い済みとして通す
- **できない操作はボタンを隠さず、試させて理由を返す。** 隠すと「なぜ消せないのか」が伝わらないうえ、隠す判断は画面を開いた瞬間の値でしかなく、送信までの隙間が残る。Action は直接叩けるので、どのみち捕まえる側が要る。隠すのは UX、捕まえるのは正しさ
- **`try` の範囲は DB を叩く 1 行だけにする。** `notFound()` と `redirect()` は例外を投げて制御するので、catch の範囲に入れると条件を広げたときに飲み込む。そのために `const` ではなく `let` で受ける（`deleteArticle`）
- **`useActionState` の結果をリセットするには `key` を変えて作り直す。** リセットの API は無い。表示フラグを別に持つと「エラーがあるか」の情報源が 2 つになり、ずれると出なくなる。リセットしたい状態を持つコンポーネントごと切り出して `key` を渡す（`DeleteArticleActions`）

### ステップ4 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 4-1 | `cacheComponents: true` を有効にする | 完了 |
| 4-2 | ダッシュボードを prerender の対象外にする | 完了 |
| 4-3 | 公開記事の一覧（`use cache`） | 完了 |
| 4-4 | `cacheTag` / `updateTag` でキャッシュを落とす | 完了 |
| 4-5 | 記事詳細ページと Markdown レンダリング | 完了 |
| 4-6 | 抜粋の切り出し（純粋関数 ＋ 単体テスト） | 完了（12 ケース。変異 5 種すべて検知） |
| 4-7 | ペイウォール（サーバー側で本文を切る） | 完了（レスポンスに全文が含まれないことを確認済み） |
| 4-8 | DAL への集約 | 完了 |

`cacheComponents` について実測した結論:

- **`instant = false` は「PPR をやめる」設定ではない。** PPR は有効なまま、静的シェルが空でもエラーにしない設定。`prerender-manifest.json` ではダッシュボードも `/login` も全部 `PARTIALLY_STATIC` になる
- ダッシュボード配下の静的シェルはすべて 0 バイト（実測）。`/login` は 11KB、`/signup` は 12KB
- **`revalidateTag` は Next.js 16 で第2引数（寿命プロファイル）が必須になった。** Server Action から即座にキャッシュを落とす用途は `updateTag(tag)` が担う（`next/cache`）。以前の知識で `revalidateTag(tag)` と書くと引数不足で落ちる
- **キャッシュのパージは迷ったら多めに落とす。** 余分な再生成はクエリ1本ぶんだが、落とし漏れは古いデータを黙って返し続け、エラーも出ずテストも通る。ただしこの非対称性は再生成が安いうちの話
- **公開 URL は `/articles/[id]`。** 著者の UUID は `auth.users` の ID そのものなので公開 URL に出さない。`/[username]/[id]` にする場合は username の予約語チェック（`login` / `dashboard` など）がセットで必要
- **文字数の数え方が JS と Postgres で違う。** JS の `.length` は UTF-16 のコード単位（絵文字 1 つ = 2）、Postgres の `char_length` は文字（= 1）。**下限の判定では JS の方が甘くなり、zod を通って DB の check 制約で落ちる。**`src/actions/articles/count-characters.ts` の `countCharacters`（コードポイントを数える）で一致させている。文字数の規則を足すときは必ずこれを使う。上限（`varchar` など）は JS の方が厳しくなるので安全側
- **`sql` テンプレートに JS の値を埋めると `$1` のプレースホルダになる。** DDL には値が入らないので check 制約では使えない。`sql.raw(String(定数))` を**数値の部分にだけ**使い、カラム参照は `${t.column}` のまま残す（そうしないとカラム名の変更に Drizzle が追随しない）。**生成された SQL を読まずに `db:migrate` しない**
- **有料記事には最低本文長（1000 文字）がある。** 抜粋（上限 400）は上限に収まれば本文をそのまま返すため、本文が短い有料記事は全文が読めてしまう。入口で弾いている。zod と DB の check 制約の両方に置き、定数は `src/db/schema.ts` から export している
- **ビルド出力の記号は当てにしない。** `/dashboard/articles/[id]/edit` は `◐` と表示されるが、シェルは他と同じく 0 バイトで実体は `ƒ` と変わらない。動的パラメータを持つルートは manifest の `dynamicRoutes` に入り、`hasEmptyStaticShell` の集計から外れる経路があるため（`build/index.js` の `if (isDynamicRoute(page) && route.pathname === page) continue;`）。判断するときは HTML のバイト数と manifest を見る

### ステップ3 の記録

| # | 内容 | 状態 |
| --- | --- | --- |
| 1 | ダッシュボードに自分の記事一覧を出す | 完了 |
| 2 | 記事の新規作成フォーム（画面） | 完了 |
| 3 | 作成の Server Action（zod 検証 ＋ 認可） | 完了 |
| 4 | 一覧への反映（`revalidatePath`） | 完了（下記の結論により何も追加しない） |
| 4.5 | `articleFormSchema` の単体テスト | 完了（21 ケース。変異 10 種すべて検知） |
| 5 | 編集フォームと更新 Action（**所有権の確認**） | 完了 |
| 6 | 削除 Action（**所有権の確認**） | 完了 |
| 7 | 認可判定のテスト | 完了（E2E に持ち越すと決定。下記） |

ステップ3 の完了条件はすべて満たした。

**認可はクエリの `where` に置く方針にした。** 記事の取得・更新は `and(eq(articles.id, id), eq(articles.authorId, user.id))` で絞り、`returning` / 取得結果が空なら `notFound()`。「存在しない」と「他人のもの」が区別できず、記事 ID の存在有無が漏れない。取得してから比較する形にすると、確認と更新の間に記事が消えた場合を取りこぼす。

**認可の自動検証はステップ10 の E2E に持ち越すと決めた。** 判断が SQL の条件に埋まっていて Vitest だけでは検証できず、実 DB を使う結合テストは `profiles.id` が `auth.users` を参照している都合でローカル Supabase かテスト専用プロジェクトが必要になり、ステップ10 と同規模の環境構築になるため。**判定を純粋関数に戻す案は退行なので採らない。**それまでの根拠は手動確認（別ユーザーで他人の記事 ID を指定し、編集ページ・更新・削除がいずれも 404 になることを確認済み）。

サブステップ4 で確かめた結論（ステップ4 で必要になる）:

- ルートを動的にするのは Dynamic API（`cookies()` / `headers()` / `searchParams` など）であって、DB アクセスではない。`db.select()` は Next.js から見えない
- `/dashboard` が毎回サーバー実行されるのは `createClient()` が `cookies()` を読むから。`npm run build` のルート一覧で `ƒ` と表示される
- Dynamic API を使わないページで Drizzle のクエリを書くと、結果がビルド時に静的 HTML へ焼き付く（`/cache-test` で実測。`.next/server/app/cache-test.html` に件数が literal で埋まっていた）
- そのため現時点の `createArticle` に `revalidatePath("/dashboard")` を書いても消す対象が無い（Data Cache も Full Route Cache もエントリ無し、Router Cache は動的セグメントの staleTime が 0）。**キャッシュを入れるステップ4 で改めて置き場所を考える**

### DAL と DB テストについて（ステップ4 で確立した）

- **`@/db` を import できるのは `src/lib/dal` の中だけ。** ESLint の `no-restricted-imports` で強制している。禁止対象は DB クライアントだけで `@/db/schema` は許す（テーブル定義は説明であって、クエリを実行する力を持たない）。対象外はテスト基盤 2 つ（`src/test/fixtures.ts` と `vitest.db.setup.ts`）
- **DAL のファイルは「認可を誰が持つか」で分ける。** `published-*`（不要）/ `my-*`（関数が `requireUser()` で持つ）/ `signup.ts`（呼び出し側が持つ＝危険なので用途を名前で限定）。**キャッシュできるものは認可が要らず、認可が要るものはキャッシュできない**（`use cache` は「誰が見ても同じ結果」でないと成立しないため）
- **DB に接続するテストは `npm run test:db`。** `npx supabase start` が必要。ファイル名は `*.db.test.ts`。詳細は README
- **Vitest では `"use cache"` の変換が走らず `cacheTag()` が落ちる。** `next/cache` をスタブに差し替えて素のクエリを検証する。`requireUser()` も `cookies()` を読めないので `@/lib/current-user` をスタブに差し替え、テストが `signInAs()` で「今は誰か」を指定する。**差し替えているのは認証であって認可ではない**
- **Vite の `alias` は先に一致したものが使われる。** `"@/"` の総称を個別の差し替えより前に置くと本物が読み込まれる。配列で順序を明示している
- **Drizzle はドライバのエラーを `DrizzleQueryError` で包む。** `e instanceof postgres.PostgresError` は真にならない。`e.cause` を見る。この判定は DB テストで固定済み

### 保留している課題

着手すると決めた順。

1. **プロフィール設定画面の実装と、アバター画像**（アバターは 2026-09-15 にステップ9 から移した）。`/dashboard/profile/new` は仮置き。`username` の検証規則を `sign-up-schema.ts` と共有する必要がある（規則がずれると「サインアップでは取れないが設定画面では取れる名前」が生まれる）
   - アバターの材料はステップ9 でそろっている。`{userId}/covers/` と同じ形で `{userId}/avatars/` を使えば、**ポリシーは 1 文字も変えずに済む**（見ているのは先頭のフォルダ名だけ）
   - `validateImageFile` と `coverImageUrl` はそのまま使える。**`coverImageUrl` は名前が `covers` 専用に見えるので、そのとき名前を見直す**
   - `profiles` に列を足すとき、`articles` と同じ形の check 制約（先頭のフォルダ名 = 自分の ID）を張るか決める
2. **ログイン画面への復帰導線**。`?next=` で元の場所へ戻す（**オープンリダイレクトの検証が必須**）、セッション切れの理由表示
3. **書きかけの記事を失わない仕組み**。セッション切れやエラーで本文が消える
4. **編集画面のクライアントキャッシュ**（2026-08-30 に発見、対処は保留）。編集 → 保存 → ダッシュボード → 同じ記事の編集画面へ**クライアント遷移**すると変更前の内容が出る。リロードで直り、5 分（`x-nextjs-stale-time: 300`）で自然に消える。編集ページはサーバー側でキャッシュしていないので、古いのはブラウザの Router Cache
   - `updateTag` はサーバーの `use cache` エントリしか落とさない。クライアント側は `refresh()`（`next/cache`、Server Action 専用）の担当だが、**この構成では使えない**
   - `refresh()` は `/dashboard` への遷移を検証し直す。そこで `requireUser()` → `supabase.auth.getUser()` の**キャッシュされない `fetch`** が `<Suspense>` の外にあるためエラーになる
   - `instant = false` は**ナビゲーション検証を免除しない**。Next.js はこの宣言を 2 つの別々の走査で読む（2026-09-10 に 16.3.4 のソースで確認）
     - `isPageAllowedToBlock`: 「ブロックしてよいルートか」。`instant === false` を見つけた時点で打ち切る。**layout の宣言で足りる**
     - `anySegmentNeedsInstantValidation`: 「検証が要るか」。`instant === false` の segment は自分では検証を要求しないが、**子を辿り続ける**。`isImplicitValidationSegment`（`__PAGE__` / `__DEFAULT__`）に当たると、宣言の無いページで暗黙に有効になる。**だから各ページにも `export const instant = false` が要る**（ダッシュボード配下 6 ページに置いた。無いと dev のログに `blocking-prerender-dynamic` が出続ける）
     - dev だけに出て build で出ないのは、既定の `validationLevel` が `warning`（level 0）で、dev の検証が level 0、build が level 1 のため。どちらも `node_modules/next/dist/server/app-render/instant-validation/instant-config.js`
     - **ログは消えたが、原因（`getUser()` が `<Suspense>` の外にある）は残っている。** 消したのは診断であって、クライアント遷移が即座でないことは変わらない
   - 対処するなら認証の構造から。`requireUser()` を `<Suspense>` に入れる（未ログイン時に枠が一瞬見える）／`<Link prefetch={false}>`／`experimental.staleTimes` のいずれか
5. **`requireUser()` がリクエストごとに Supabase へ HTTP 往復している。** Cookie を読むだけの処理ではない。`supabase.auth.getUser()` は Auth サーバーに問い合わせる。React の `cache()` で 1 リクエスト 1 回には畳んであるが、往復自体は残る
6. **拒否した理由がサーバー側にも残らない**（2026-09-03 に発見）。`createCheckout` は「記事が無い」と「あるが買えない（自分の記事・購入済み・無料）」の両方を `notFound()` で返す。画面に同じ答えを返すのは正しい（記事 ID の存在有無を漏らさない）が、ログにも区別が残らないため、購入できないという問い合わせが来ても原因を追えない。**画面の答えは 404 のまま、サーバーのログにだけ理由を残す**のが対処。同じ形は `updateArticle` / `deleteArticle` にもある
7. **著者が売った記事を引っ込める手段が無い**（2026-09-03 に発見）。購入行がある記事は外部キーで削除できず（それが正しい）、`status` を `draft` に戻すと `getPublishedArticle` / `getLatestArticles` の `where` に引っかかって**購入者まで 404 になる**。つまり「販売は止めたいが購入者には読ませ続ける」ができない。必要なのは「一覧には出ないが URL は生きていて、購入者は読める」状態で、次がセットになる
   - `articles` に状態を 1 つ増やす（`status` の値を足すか、別の列にするか）
   - `getLatestArticles` の条件を変える。詳細ページの URL は生かす
   - `canPurchase` に「新規購入を止める」条件が増える
   - `status` の列挙値を増やす形にすると `published_requires_date` の check 制約と `articleFormSchema` にも波及する
   - **note の仕様が参考になる**（2026-09-09 に検討）。「販売停止」＝公開のまま購入ボタンだけ消える、「削除」＝一覧から消えるが購入者は URL から読める、「下書きに戻す」＝一度でも購入された有料記事では不可、の 3 つに分かれている
   - **「購入者だけが読める」は `use cache` の前提を壊す。** `getPublishedArticle` は「記事 ID が決まれば誰が見ても同じ」を前提にキャッシュしている。しかも**ストリーミングが始まった後に `notFound()` で HTTP ステータスは変えられない**ので、購入者以外に返せるのは「404 に見える画面」であって本物の 404 ではない。それを許容するか、履歴からのリンク先を認証必須の別ルートにするかが分かれ目
8. **導線で手を付けていないもの**（2026-09-09 時点）。公開ページの共通ヘッダー、ペイウォールのログイン導線、ダッシュボード ⇔ 購入履歴は入れた。残っているのは次の 2 つ
   - **記事詳細に著者名が出ない。** 出すには `getPublishedArticle` に `profiles` の join が要る（学習範囲）
   - **`/success` の「確認中」と「未確認」の画面から記事へ戻れない。** 完了画面にしかリンクが無い
   - なおダッシュボード配下の共通ナビは置いていない。行き先が 2 つのうちはページ内のリンクで足りる。**ステップ8 で売上ダッシュボードが増えて 3 つになった時点で設計する**
9. **売上の集計期間を選べない**（2026-09-09 に決定、着手は保留）。ステップ8 の月別推移は**直近 12 か月の固定**で作る。理想は期間選択と全期間だが、次がセットになる
   - `searchParams` が入るので、そのページのキャッシュの扱いを決め直すことになる
   - 全期間を許すと行数が運用年数に比例して増える。グラフの横軸も潰れる
   - 集計の起点は**基準日を引数で受け取る**形にしてあるので、DAL 側はそのまま拡張点になる（既定値は `new Date()`）。DB の `now()` に頼ると、12 か月の窓が動いてフィクスチャが**年が変わった瞬間に窓から外れる**
10. **`src/lib/dal/checkout.ts` にテストが無い**（2026-09-09 に気づいた）。DAL で唯一テストの無いファイルで、しかも**金額の正を取りに行く場所**
   - 固定したいのは `getArticleForCheckout` が **`status = 'published'` の記事しか返さない**こと。Server Action は UI を経由せず直接叩けるので、下書きの記事 ID を渡す経路は実在する
   - これは「購入がある記事の `publishedAt` は必ず入っている」という不変条件の**根**でもある。この不変条件は `getArticleForCheckout` の絞り込み・check 制約 `published_requires_date`・`updateMyArticle` が `publishedAt` を消さないこと、の 3 つに分かれて支えられていて、どこにも名前が付いていない
   - **他の関数にコメントで書いても守れない。** 変更する人はそのファイルを読まない。固定するならこのテスト
11. **同時に公開すると、一覧に反映されない記事が残ることがある**（2026-09-17 に E2E で実測）。`use cache` と `updateTag` の順序の問題
   - 「A が記事を作って `updateTag` でパージ → **その前に始まった B の一覧生成**が、パージの後に結果を保存」で、A の記事を含まない結果がキャッシュに入る
   - 直るのは次に誰かが `updateTag` を呼ぶか、`cacheLife`（15 分）が切れたとき。**詳細ページは開けるので実害は小さい**
   - E2E はこれを避けるため `workers: 1`（`playwright.config.ts` にコメントあり）。**依存が無いことと、同時に走れることは別**
12. **アップロードした画像の孤児が残る**（2026-09-14 に整理）。Storage にファイルだけが残り、どこからも参照されない状態。出どころが 3 つある
   - **記事を保存せずに離脱**。送信時にアップロードする形にしたので減らしたが、**アップロード成功 → Server Action がバリデーションで落ちる**経路が残る。さらにそこで選び直すと、前に上げたぶんが孤児になる
   - **記事の削除**。`deleteArticle` は Storage に触っていない
   - **差し替えの `remove()` が失敗**。巻き戻さない判断なので、パスをログに残してある（`Article ID: … Image Path: … error.name: message`）。**このログ以外に手がかりは無い**
   - 掃除の口は「`storage.objects` にあって `articles.cover_image_path` のどこにも無いパス」。Drizzle から `storage.objects` を読むのは DAL の境界の外なので、どこに置くかは要検討

### 決まっている方針

- **Next.js は 16.3.4**（2026-09-10 に 16.3.1 から更新）。`next 16.0.0 - 16.3.2` の critical 勧告（Image Optimization API の AVIF 経由 RCE）を踏むため、ステップ9 で `next/image` を使う前に上げた。`prerender-manifest.json` の `renderingMode` は全ルート差分なし、静的シェルも 15〜45 バイト縮んだだけで構造は同じ。`cacheComponents` の挙動は変わっていない。**`next` と `eslint-config-next` は完全固定のまま**にしてある（`npm install next@…` は `^` を付けるので戻すこと）
- ステップ4 の開始時に `cacheComponents: true` を有効にする（詳細はステップ4 の節）
- 確認メールは無効化済み（詳細は「外部サービスの準備」の節）
- **認可は layout に置かない。** Next.js の layout はクライアント側の画面遷移で再レンダリングされないため、layout だけのチェックは「一度通れば以降は確認されない」状態になる。各ページ・各 Server Action の入口に `requireUser()` を置く。重複するコストは `src/lib/current-user.ts` の React `cache()` が吸収する（1 リクエスト＝ `getUser()` 1 回）
- **`cache()`（React）と `use cache`（Next.js）は別物。** 前者はリクエスト内の重複排除でリクエストをまたがない。モジュールのトップレベル変数でキャッシュすると Node.js のプロセス全体で共有され、別ユーザーに漏れる
- **DAL はステップ4 の頭で作る。** 現時点で複数箇所から呼ばれるのは `requireUser()` だけなので `src/lib/current-user.ts` に置いている。ステップ4 で記事クエリ・閲覧可否判定・購入済み判定が増えた時点で `src/lib/dal/` に集約する。DAL は「そこを通らないとデータに触れない」ことで初めて意味を持つので、`@/db` を DAL 以外から import 禁止にする ESLint ルール（`no-restricted-imports`）とセットで導入する

## Claude への禁止事項

- 学習者が書くべきコード（スキーマ・サーバー処理・認可・ストレージポリシー・UI ロジック・テスト）を、依頼されていないのに書く
- 現在のステップより先を実装する・提案する
- 依頼されていないリファクタリングを行う
- 複数のファイル・複数の手順を一度にまとめて指示する（「指導の粒度」を守る）
- 確認方法を示さないまま次のステップへ進ませる
- 「ついでに」ライブラリを追加する（「使ってよいライブラリ」の範囲でも、必要になるステップより前に入れない）
- エラーの解決策として、修正済みコード全文をいきなり提示する（まず原因を説明する）
- DB テーブルの読み書きに Supabase クライアント（`supabase.from(...)`）を使う構成を提案する（DB は Drizzle に統一）
- 「RLS があるからアプリ層の認可は省略してよい」という説明をする
- **認可チェックや署名検証を省いた「とりあえず動く」サンプルを提示する**（例示コードであってもセキュリティ原則を満たしたものだけを示す）
- ペイウォールの実装としてクライアント側での出し分けを許容する
- ストレージポリシーを「全許可」にして先へ進める提案をする（一時的なデバッグ目的でも、戻す手順とセットでなければ提案しない）
- 本物の API キーやシークレットをコード・ドキュメント・コミットに書く（発見したら即座に指摘する）
- README.md に実装ステップの答えやサンプルコードを書く

## 学習者が詰まったときの質問テンプレ

```
ステップ: {{番号}}
やろうとしたこと:
書いたコード:
期待した動作:
実際の動作 / エラー:
自分の仮説:
```
